{% extends 'base.html.twig' %}

{% block title %}Extra{% endblock %}

{% block body %}
    <h1>Extra</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ extra.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ extra.price }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ extra.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_extra_index') }}">back to list</a>

    <a href="{{ path('app_extra_edit', {'id': extra.id}) }}">edit</a>

    {{ include('extra/_delete_form.html.twig') }}
{% endblock %}
