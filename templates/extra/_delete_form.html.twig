<form method="post" action="{{ path('app_extra_delete', {'id': extra.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ extra.id) }}">
    <button class="btn">Delete</button>
</form>
